using AutoMapper;

namespace Mph.EFCore.Infrastructure
{
    public class AutoMapperProfile : Profile
    {
        public AutoMapperProfile()
        {
            // 数据库实体 → DTO

            // DTO → 数据库实体（新增/修改用）
        }
    }
}
